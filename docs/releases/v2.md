# Village Survivor — Release v2

> Statut : **candidate technique revue favorable**, validation LAN finale en cours
> Date : 3 août 2026
> Environnement automatique : Windows, Node 24/26, Chromium, Docker LAN
> Niveau de garantie requis : `renforce`

## Résultat livré

La v2 remplace entièrement la coopération lockstep par un serveur autoritaire commun au solo et
à la coopération. Le navigateur n'exécute plus les règles, ne choisit plus la seed et ne crédite
plus l'or. Le serveur Colyseus valide les commandes, conserve l'état, gère la reconnexion et
finalise les récompenses.

La ferraille au sol est bornée : seules les morts de monstres créent un tas, et tout tas non
ramassé expire après 600 ticks. Les quêtes continuent de verser directement dans la caisse
commune.

## Preuves techniques acquises

| Gate | Résultat |
|---|:---:|
| Ferraille : source, expiration, priorité de ramassage, quêtes | **PASS** |
| Solo et coop 2/4 clients sur vrai serveur | **PASS** |
| Roster partiel 15 s, reconnexions 10/31 s, départs | **PASS** |
| Panne serveur et panne OTLP sans fallback local | **PASS** |
| Deux finalisations PostgreSQL concurrentes et droits RPC | **PASS** |
| Conteneur sain, proxy `/game/health`, limite 512 Mio | **PASS** |
| Charge 24 000 ticks, 4 joueurs, 200 monstres | **PASS** — runtime p95 0,169 ms, projection p95 48 Kio |
| Commande→état sur clients Colyseus locaux | **PASS** — p95 143,0 ms |
| Revue Claude indépendante | **FAVORABLE** — aucun P0–P2, quatre P3 corrigés et retestés |

Les totaux de la suite complète, le benchmark final et l'arbitrage Claude sont consignés dans
[`../qualite/rapport-tests.md`](../qualite/rapport-tests.md).

## Changements d'exploitation

- service Docker `game-server` limité à 512 Mio et contrôlé par `/health` ;
- proxy HTTP/WebSocket Nginx sous `/game/` ;
- migration `0006` additive, puis révocation de `credit_account_gold` pour `authenticated` ;
- racine `game.room`, propagation W3C depuis la création client et métriques agrégées ;
- rooms en mémoire : un redémarrage interrompt la session et ramène au lobby ;
- état terminal conservé 60 secondes, retry idempotent de persistance toutes les 5 secondes.

## Gate encore ouverte

La release ne sera déclarée prête qu'après une partie solo et une partie coopérative sur deux
postes LAN, avec lecture dans le backend d'une trace distribuée complète. Cette preuve réelle ne
peut pas être remplacée par les clients locaux automatisés.
